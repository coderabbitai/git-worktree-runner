# git-worktree-runner
Git worktree helper for parallel Claude Code sessions. Creates per-branch worktrees (mono-2, mono-3…), opens GitHub Desktop, copies .env/CLAUDE.md, installs deps, runs turbo build, and adds shortcuts (cd/claude/cursor/desktop/list/rm).
